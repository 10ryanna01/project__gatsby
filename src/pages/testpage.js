import React from "react"

export default () => {
    return(
        <div>

            this is a test page to test the autorouting form gatsby
            this is a test page to test the autorouting form gatsby
            this is a test page to test the autorouting form gatsby
            this is a test page to test the autorouting form gatsby
        </div>
    )

}